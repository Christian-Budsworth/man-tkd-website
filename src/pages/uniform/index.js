import * as React from "react";
import Layout from "../../component/layout";

export default function uniformPage () {
    return (
        <Layout pageTitle="Uniforms">
          <h1>Uniforms</h1>
        </Layout>
    );
  };
